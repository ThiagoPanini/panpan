# GitHub Personal Access Token — used at build time to fetch
# repo metadata, commit activity and contribution calendar.
# Scope needed: read:user (public_repo is already implicit for public data).
# Generate at: https://github.com/settings/tokens
GITHUB_ACCESS_TOKEN=
